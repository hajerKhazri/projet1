{% extends 'base.html.twig' %}

{% block title %}RDV{% endblock %}

{% block body %}
    <h1>RDV</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ r_d_v.id }}</td>
            </tr>
            <tr>
                <th>Dateheure</th>
                <td>{{ r_d_v.dateheure ? r_d_v.dateheure|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ r_d_v.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_r_d_v_index') }}">back to list</a>

    <a href="{{ path('app_r_d_v_edit', {'id': r_d_v.id}) }}">edit</a>

    {{ include('rdv/_delete_form.html.twig') }}
{% endblock %}
