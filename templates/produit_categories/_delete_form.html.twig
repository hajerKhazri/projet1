<form method="post" action="{{ path('app_produit_categorie_delete', {'id': produit_categorie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ produit_categorie.id) }}">
    <button class="btn">Delete</button>
</form>
